# LangChain Examples

This project has some jupyter notebook of how to use LangChain to work with LLMs

## Setup

To configure the environment using coda, run the following command:

~~~bash
conda env create --file=langchain_ai.yaml
~~~